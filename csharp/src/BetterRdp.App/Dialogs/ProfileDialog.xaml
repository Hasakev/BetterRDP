<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="BetterRdp_App.Dialogs.ProfileDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Add display profile"
    PrimaryButtonText="Add"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    mc:Ignorable="d">

    <StackPanel Spacing="12" Width="380">
        <TextBox x:Name="NameInput" Header="Name" />

        <ComboBox
            x:Name="ModeInput"
            Header="Mode"
            HorizontalAlignment="Stretch"
            SelectionChanged="OnModeChanged" />

        <StackPanel x:Name="MonitorsRow" Spacing="6">
            <TextBlock Text="Monitors" Style="{ThemeResource BodyStrongTextBlockStyle}" />
            <StackPanel x:Name="MonitorsPanel" Orientation="Horizontal" Spacing="12" />
        </StackPanel>

        <StackPanel x:Name="ResolutionRow" Orientation="Horizontal" Spacing="8" VerticalAlignment="Bottom">
            <NumberBox
                x:Name="WidthInput"
                Header="Width"
                Value="1920"
                Minimum="640"
                Maximum="7680"
                SpinButtonPlacementMode="Compact" />
            <TextBlock Text="×" VerticalAlignment="Bottom" Margin="0,0,0,8" />
            <NumberBox
                x:Name="HeightInput"
                Header="Height"
                Value="1080"
                Minimum="480"
                Maximum="4320"
                SpinButtonPlacementMode="Compact" />
        </StackPanel>

        <ComboBox
            x:Name="ScaleInput"
            Header="Scale"
            HorizontalAlignment="Stretch" />
    </StackPanel>
</ContentDialog>
